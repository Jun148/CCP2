<form method="post" action="{{ path('head_shop_delete', {'id': head_shop.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ head_shop.id) }}">
    <button class="btn btn-primary">Delete</button>
</form>

{% extends 'baseadmin.html.twig' %}

{% block title %}ProductCare{% endblock %}

{% block body %}
<div class="show">
    <h1>ProductCare</h1>
    <br>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_care.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ product_care.Title }}</td>
            </tr>
            <tr>
                <th>Subtitle</th>
                <td>{{ product_care.Subtitle }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ product_care.Text }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td><img src="{{ vich_uploader_asset(product_care.image, 'imageFile') }}"></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('product_care_index') }}">back to list</a>

    <a href="{{ path('product_care_edit', {'id': product_care.id}) }}">edit</a>

    {{ include('product_care/_delete_form.html.twig') }}
</div>
{% endblock %}

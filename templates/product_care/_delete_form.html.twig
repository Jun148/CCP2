<form method="post" action="{{ path('product_care_delete', {'id': product_care.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_care.id) }}">
    <button class="btn btn-primary">Delete</button>
</form>

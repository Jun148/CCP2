{% extends 'baseadmin.html.twig' %}

{% block title %}Edit Gender{% endblock %}

{% block body %}
<div class="edit">
    <h1>Edit Gender</h1>
    <br>
    {{ include('gender/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('gender_index') }}">back to list</a>

    {{ include('gender/_delete_form.html.twig') }}
</div>
{% endblock %}

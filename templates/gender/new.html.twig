{% extends 'baseadmin.html.twig' %}

{% block title %}New Gender{% endblock %}

{% block body %}
<div class="addnew">
    <h1>Create new Gender</h1>
    <br>

    {{ include('gender/_form.html.twig') }}

    <a href="{{ path('gender_index') }}">back to list</a>
</div>
{% endblock %}

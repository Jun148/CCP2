{% extends 'baseadmin.html.twig' %}

{% block title %}Links{% endblock %}

{% block body %}
<div class="show">
    <h1>Links</h1>
    <br>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ link.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ link.Name }}</td>
            </tr>
            <tr>
                <th>Path</th>
                <td>{{ link.Path }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('links_index') }}">back to list</a>

    <a href="{{ path('links_edit', {'id': link.id}) }}">edit</a>

    {{ include('links/_delete_form.html.twig') }}
</div>
{% endblock %}

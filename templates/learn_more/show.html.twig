{% extends 'baseadmin.html.twig' %}

{% block title %}LearnMore{% endblock %}

{% block body %}
<div class="show">
    <h1>LearnMore</h1>
    <br>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ learn_more.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ learn_more.Title }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ learn_more.Text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('learn_more_index') }}">back to list</a>

    <a href="{{ path('learn_more_edit', {'id': learn_more.id}) }}">edit</a>

    {{ include('learn_more/_delete_form.html.twig') }}
</div>
{% endblock %}

<form method="post" action="{{ path('learn_more_delete', {'id': learn_more.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ learn_more.id) }}">
    <button class="btn btn-primary">Delete</button>
</form>
